# --- core scientific stack (same pins as requirements.txt) ---
pandas==1.3.5
numpy==1.21.6
tqdm==4.64.1
matplotlib==3.5.3
scikit-learn==1.0.2
goatools==1.2.3
scipy==1.7.3
requests==2.28.2

# --- deep learning: CPU builds (the CUDA variants live in requirements.txt) ---
torch==1.8.1
dgl==0.5.2

# --- PyG, needed for the disease-area splits ---
# (cell_proliferation, mental_health, cardiovascular, anemia, adrenal_gland,
#  autoimmune, metabolic_disorder, diabetes, neurodigenerative)
#
# The other splits (complex_disease, random, full_graph) do not need these,
# because the import is lazy, inside methods in txgnn/data_splits/datasplit.py.
#
# The -f line is REQUIRED: PyPI only has source tarballs for torch-scatter and
# torch-sparse at these versions, so without it pip tries to compile from source.
# Verified installing and running on macOS x86_64 / Python 3.8 / torch 1.8.1.
-f https://data.pyg.org/whl/torch-1.8.0+cpu.html
torch-scatter==2.0.7
torch-sparse==0.6.9
torch-geometric==2.0.4
